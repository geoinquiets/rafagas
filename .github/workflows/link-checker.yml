name: Rafagas workflow
on:
  pull_request:
    branches:
      - gh-pages
jobs:
  link-checker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install libcurl4-openssl-dev
        run: sudo apt-get update && sudo apt-get install -y libcurl4-openssl-dev

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.4.1
          bundler-cache: true

      - name: Cache folders
        uses: actions/cache@v2
        with:
          path: |
            tmp
            _site
          key: rafagas-cache

      - name: Build the site
        run: bundle exec jekyll build
        env:
          NOKOGIRI_USE_SYSTEM_LIBRARIES: true

      - name: Run the link check
        id: link-check-run
        run: |
          bundle exec ruby script/proof-all.rb 2>&1 | grep -oP 'External link \K.*+' >> output.log || true
          echo "::set-output name=result::$$(cat output.log | wc -l)"

      - name: Read links
        id: broken-links
        uses: juliangruber/read-file-action@v1
        with:
          path: output.log

      - name: Add comment to PR
        if: github.event_name == 'pull_request'
        env:
          URL: ${{ github.event.pull_request.comments_url }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl \
            -X POST \
            $URL \
            -H "Content-Type: application/json" \
            -H "Authorization: token $GITHUB_TOKEN" \
            --data "{ \"body\": \"### ${{ steps.link-check-run.outputs.result }} broken links\n\n```\n${{ steps.broken-links.outputs.content }}\n```\" }"

      # - name: Check outputs
      #   run: |
      #     cat output.log
      #     if [ ${{ steps.link-check-run.outputs.result }} -ne 0 ] 
      #     then
      #       exit 1
      #     fi
